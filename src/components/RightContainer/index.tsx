import React from 'react';
import { ConfigContainer, AppPromotion, ToTop } from './styles';

const RightContainer: React.FC = () => {
  return (
    <ConfigContainer>
      <AppPromotion>
        <span></span>
      </AppPromotion>
      <ToTop />
    </ConfigContainer>
  );
}

export default RightContainer;
